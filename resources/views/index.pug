extends extends/layout

block content
  .section.section_work.off
    .work
      each job, name in config.work
        .job(class='job_' + name)
          .inner.layer(data-depth=0.05)
            .background.layer(data-depth=0.1)
            .logo.layer(data-depth=0.2)
            .title: .copy=job.title
  .section.section_code.off
    .uc coming soon
  .section.section_contact.off
    .uc coming soon

  script.
    Index.i()
